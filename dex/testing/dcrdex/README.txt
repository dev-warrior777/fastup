
This is a "drop-in" replacement for dcrdex/harness.sh

It requires DCR simnet harness to be up plus at least one more harness
to make market(s)

It does not require BTC harness to be running but will use it if it is
